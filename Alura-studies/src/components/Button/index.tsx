import botao from "./Botao.module.scss";

const Botao = () => {
  return <button className={botao.botao}>Botão</button>;
};

export default Botao;

// Não precisa usar exatamente style, qualquer nome dado no import funciona.
